Blind SSRF with out-of-band detection:

    -Blind vulnerability means the application does not return the response.
    -By pointing the request to a Burp Collaborator server, We can confirm that the application is making backend requests but we cannot see the content of those requests directly.
    -