File path traversal, validation of start of path:
    -In some application,the url will be accepted only when the standard file path or verified file path
    -To bypass this we use standard file path and followed by traversal sequence and the required file
    -filname=/var/www/img/../../../etc/passwd