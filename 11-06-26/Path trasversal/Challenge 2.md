File path traversal, traversal sequences blocked with absolute path bypass:
    -In application the sequence ../../../ are blocked or striped off due to the defence of the application
    -By using absolute path we can bypass the defence because it is bypassing directly.
    -filename=/etc/passwd is the absolute file path 