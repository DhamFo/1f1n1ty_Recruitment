Finding a hidden GraphQL endpoint:

    -In this we need to find the hidden GraphQL endpoint there are some common endpoints are there try tme and i get in /api query not found
    -So i send that to repeter and added the scema part in url and now i am able to use introspection.
    -After that i sent that to target find two url endoded 
        -For delete
        -For user id 
    -Tried second one for password but in that field there is no name called password 
    -So i tried first one and successfully deleted the username carlos 