Accidental exposure of private GraphQL fields:

    -In this challenge we need to get the password for the admin username
    -The request of login is sent to repeater and set introspection query and sent that to target 
    -There chk the query getUser and the field that it having copy and past it in the GQl section and make thee id to 1 for the admin password 