Bypassing GraphQL brute force protections:

    -In this we burtforce the given possible password to login as carlos
    -We need to copy the script given in the tip and paste it in the console of the browser.
    -And we passed the request to repeter,change the GQL code and send for that to response.