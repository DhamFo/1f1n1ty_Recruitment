Performing CSRF exploits over GraphQL:

    -Here we combine GraphQL and CSRF to completel the challenge 
    -We first the request from POST to POST to get content-type while we converting the query part will be deleted so we need to paste the  query part as encoded one and store it in a variable
    -Now we send to response once done right click and some settings for CSRF html code to store in exploit and deliver to victm.