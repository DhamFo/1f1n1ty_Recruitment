Monoprime:
    -This challenge has one factor for n and every thing is same after finding the factor
    -If n is easy factorizable then we can find private key with public key so that we need two factor for security