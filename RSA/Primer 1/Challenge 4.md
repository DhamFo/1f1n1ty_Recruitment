Square eyes:
    -The vaue of n is square of one 1024-bit number so p and q are same in this challenge
    -So phi(n)=phi(p**2)=p*(p-1)
    -In this we use phi(n)=p*(p-1) insted of phio(n)=(p-1)*(q-1)
    -This is the trick we use here after this every step is same as previous challenge