Reflected cross-site scripting:
    -We need to perform a cross-site scripting that calls alert() function in the search bar of the post
    -In seaech bar type <script>alert()</script>
    -The tag <script>"Command/code"</script> tells the browser to run the command immediately by the  javascript engine
    -alert() shows the pop-box which tells stop all other javascript until user click ok in the pop-box