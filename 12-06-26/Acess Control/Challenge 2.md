Unprotected admin functionality with unpredictable URL:

    -The admin functionality is in the unpredictable url so to find this we need to look after js part in htlm
    -This Challenge let us to find <script></script> tag for js where the url is stored
    -In inspect mode,lab header end part has the script tag for the url and using the url delete the account