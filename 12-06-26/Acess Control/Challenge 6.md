User ID controlled by request parameter, with unpredictable user IDs:

    -In this we should find carols API key by unpredictable user id
    -View bolg post one by one for carlos post. Click carlos name and obtainn the unpredictable id fomr url and change them in my account and submit the API key